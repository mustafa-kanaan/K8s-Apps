apiVersion: v1
kind: ConfigMap
metadata:
  name: url-shortener-config
  namespace: {{ .Values.namespace }}
data:
  DB_HOST: "mongodb-0.mongodb-headless,mongodb-1.mongodb-headless,mongodb-2.mongodb-headless"
  DB_NAME: {{ .Values.db.name }}