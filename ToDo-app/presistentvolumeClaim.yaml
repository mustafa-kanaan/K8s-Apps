apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ToDo-pvc
  namespace: hands-on
spec:
  storageClassName: "local-path"
  resources:
    requests:
      storage: 500Mi
  accessModes:
    - ReadWriteOnce
